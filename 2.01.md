Below is a **basic and commonly used Prometheus `config.yaml` (prometheus.yml)** that you can use for learning, notes, or a real setup.

---

## 📄 prometheus.yml (Sample Configuration)

```yaml
global:
  scrape_interval: 15s          # How often Prometheus scrapes targets
  evaluation_interval: 15s      # How often rules are evaluated
  scrape_timeout: 10s           # Timeout for scraping

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "localhost:9093"

# Rule files (alerting & recording rules)
rule_files:
  - "rules/*.yml"

# Scrape configurations
scrape_configs:

  # Prometheus self-metrics
  - job_name: "prometheus"
    static_configs:
      - targets:
          - "localhost:9090"

  # Node Exporter (Linux server metrics)
  - job_name: "node_exporter"
    static_configs:
      - targets:
          - "localhost:9100"

  # Kubernetes API Server
  - job_name: "kubernetes-apiserver"
    kubernetes_sd_configs:
      - role: endpoints
    scheme: https
    tls_config:
      ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
    bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
    relabel_configs:
      - source_labels: [__meta_kubernetes_namespace, __meta_kubernetes_service_name]
        action: keep
        regex: default;kubernetes

  # Application metrics
  - job_name: "my-app"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "app-server:8080"
```

---

## 🔑 Key Sections Explained (Simple)

### **global**

* Defines default scrape and evaluation intervals.

### **alerting**

* Where Prometheus sends alerts (Alertmanager).

### **rule_files**

* Alert rules and recording rules location.

### **scrape_configs**

* Defines **what Prometheus scrapes** and **from where**.

---

## 🔹 Common Exporter Examples

### Node Exporter

```yaml
- job_name: "node"
  static_configs:
    - targets: ["server1:9100"]
```

### Blackbox Exporter (HTTP / TCP checks)

```yaml
- job_name: "blackbox"
  metrics_path: /probe
  params:
    module: [http_2xx]
  static_configs:
    - targets:
        - https://google.com
  relabel_configs:
    - source_labels: [__address__]
      target_label: __param_target
    - target_label: __address__
      replacement: blackbox-exporter:9115
```

---

## 📍 Prometheus Config File Location

* **Docker**: `/etc/prometheus/prometheus.yml`
* **Kubernetes (ConfigMap)**:

```bash
kubectl get cm prometheus-server -n monitoring
```

---

## 🧪 Validate Config

```bash
promtool check config prometheus.yml
```

---

## ✅ Minimal Config (For Quick Start)

```yaml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
```

---

If you want, I can also provide:

* 🔹 **Kubernetes Prometheus ConfigMap**
* 🔹 **Alert rules YAML**
* 🔹 **Prometheus + Grafana setup**
* 🔹 **Prometheus interview notes (DevOps focused)**

Just tell me 👍
